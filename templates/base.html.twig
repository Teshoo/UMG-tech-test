<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            <style>
                body {
                    display: grid;
                    align-content: start;

                    height: 100vh;
                    padding-top: 50px;

                    background-color: #2C303F;

                    font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
                    font-size: 18px;
                    
                }
                header {
                    position: absolute;
                    left: 0px;
                    top: 10vh;

                    display: flex;
                    flex-direction: column;
                    gap: 10px;
                    padding: 20px;

                    background-color: rgba(124, 122, 234, 0.5);;
                    color: #FFF;

                    border-radius: 0 10px 10px 0;
                }
                header a:link, header a:visited {
                    color: #FFF;
                }
                header a:hover {
                    color: #BFFFFF;
                }
                .page-container {
                    display: grid;

                    width: 70%;

                    margin: auto;
                    padding: 30px;

                    background-color: #FFF;

                    border-radius: 10px;
                }
                .login-container {
                    display: grid;

                    width: 300px;
                    height: 400px;

                    margin: auto;
                    padding: 30px;

                    background-color: #FFEFD5;

                    border-radius: 20px;
                }
                .login-form {
                    display: grid;
                    gap: 20px;

                    box-sizing: border-box;
                }
                .login-form-item {
                    display: grid;
                    grid-template-rows: 1fr 1fr; 
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <header>
        <h2 style="color: #FFEFD5;">Menu</h2>
        <div><a href="{{ path('app_home') }}">Home</a></div>
        <div><a href="{{ path('app_basic_login') }}">Basic login</a></div>
        <div><a href="{{ path('app_ldap_login') }}">Ldap login</a></div>
        <div><a href="{{ path('app_profile') }}">Profil</a></div>
        <div><a href="{{ path('app_admin') }}">Admin</a></div>
    </header>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
